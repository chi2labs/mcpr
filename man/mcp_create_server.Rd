% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_server.R
\name{mcp_create_server}
\alias{mcp_create_server}
\title{Create MCP Server from Source}
\usage{
mcp_create_server(
  source,
  name = NULL,
  output_dir = "./servers",
  title = NULL,
  description = NULL,
  version = "1.0.0",
  author = NULL,
  include = NULL,
  exclude = NULL,
  recursive = TRUE,
  use_protocol = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{source}{Character string specifying the source:
\itemize{
\item Path to an R file with decorated functions
\item Path to a directory containing R files
\item Name of an installed R package
}}

\item{name}{Server name (auto-generated from source if not provided)}

\item{output_dir}{Output directory for the generated server (default: "./servers")}

\item{title}{Human-readable server title (optional)}

\item{description}{Server description (optional)}

\item{version}{Server version (default: "1.0.0")}

\item{author}{Author name (optional)}

\item{include}{Character vector of patterns to include (for packages/directories)}

\item{exclude}{Character vector of patterns to exclude (for packages/directories)}

\item{recursive}{Logical, whether to search directories recursively (default: TRUE)}

\item{use_protocol}{Logical, whether to use enhanced protocol (default: TRUE)}

\item{overwrite}{Whether to overwrite existing directory (default: FALSE)}
}
\value{
Path to the generated server directory
}
\description{
High-level API for creating MCP servers from R source files, directories, or packages.
This function parses decorators, generates a complete npm package, and sets up
all necessary infrastructure for a working MCP server.
}
\examples{
\dontrun{
# From an R file with decorators
mcp_create_server(
  source = "analysis.R",
  output_dir = "servers",
  description = "Analysis tools for data science"
)

# From a package
mcp_create_server(
  source = "ggplot2",
  include = c("ggplot", "geom_*"),
  exclude = c("*.data")
)

# From a directory
mcp_create_server(
  source = "R/",
  name = "my-tools",
  recursive = TRUE
)
}
}
